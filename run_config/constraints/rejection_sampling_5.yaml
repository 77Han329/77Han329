# @package _global_

config:
  model:
    custom_model_config:
      polytope_storage_method: seed #seed, file
      polytope_generation_method: rejection_sampling #points, rejection_sampling, unconstrained
      polytope_generation_data:
        #storage method seed
        seed: 1
        #rejection_sampling method
        number_of_constraints: 5
        bounds: [0, 1]