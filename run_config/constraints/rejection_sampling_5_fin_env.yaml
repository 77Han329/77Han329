# @package _global_

config:
  model:
    custom_model_config:
      polytope_storage_method: seed 
      polytope_generation_method: rejection_sampling_fin_env
      polytope_generation_data:
        #storage method seed
        seed: 2
        #rejection_sampling method
        number_of_constraints: 5
