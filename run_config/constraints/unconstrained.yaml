# @package _global_

config:
  model:
    custom_model_config:
      polytope_storage_method: seed
      polytope_generation_method: unconstrained
      polytope_generation_data:
        #storage method seed
        seed: 1
